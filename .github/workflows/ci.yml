name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install mold linker
        run: sudo apt-get install -y mold
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Build
        run: cargo build
      - name: Test
        run: cargo test

  deny:
    name: Deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2

  secrets:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  compliance:
    name: Compliance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Organizational-context scan
        run: |
          # Keep internal organizational framing out of public artifacts.
          # The grep terms are Japanese org-context words by design; the
          # workflow itself is excluded from the scan.
          if git grep -nI -e 自社 -e 弊社 -e 当社 -e 社内 -e 自部門 -- ':!.github/'; then
            echo "::error::Organizational-context wording detected; keep it out of this public repository."
            exit 1
          fi
      - name: Author-email allowlist
        if: github.event_name == 'pull_request'
        run: |
          # Only GitHub noreply author emails are allowed on new commits
          # (prevents leaking personal addresses).
          range="${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}"
          bad=$(git log --format='%an <%ae>' "$range" | grep -viE '@users\.noreply\.github\.com>$' || true)
          if [ -n "$bad" ]; then
            echo "::error::Commits with non-noreply author emails found:"
            printf '%s\n' "$bad"
            exit 1
          fi
